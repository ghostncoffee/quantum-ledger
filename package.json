{
  "name": "star-citizen-ledger-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "icons": "node scripts/generate-icons.js",
    "build": "npm run build:server && npm run build:client",
    "package": "npm run icons && npm run build && node scripts/prepare-electron.js && cd electron && npm install && npm run dist",
    "package:dir": "npm run icons && npm run build && node scripts/prepare-electron.js && cd electron && npm install && npm run dist:dir"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
